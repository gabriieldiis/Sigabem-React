salario_hora = float(input("Qual o seu salario por hora? "))

horas_trabalhadas = float(input("Quantas horas trabalhadas voce teve esse mês?"))

salario = salario_hora * horas_trabalhadas

print(salario)